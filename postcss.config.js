export default {
  plugins: {
    // Tailwind v4 uses a separate PostCSS plugin package. Use the new
    // package name so PostCSS can resolve it correctly.
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
}
